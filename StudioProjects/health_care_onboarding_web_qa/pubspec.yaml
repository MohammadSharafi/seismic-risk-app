name: health_care_onboarding_web_qa
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  auto_route: ^7.0.2
  flutter_staggered_grid_view: ^0.6.2
  hexcolor: ^3.0.1
  flex_color_scheme: ^7.3.1 # Added version for stability
  syncfusion_flutter_core: ^24.2.9 # Added version based on pub.dev
  shared_preferences: ^2.5.3
  google_fonts: ^4.0.5
  flutter_svg: ^1.0.3
  lottie: ^1.2.1
  flutter_secure_storage: ^9.2.4
  device_preview: ^1.2.0
  intl: ^0.19.0 # Standardized version
  flutter_libphonenumber: ^2.5.1
  cloud_firestore: ^5.6.6 # Moved from dev_dependencies
  flutter_bloc: ^8.1.1 # Moved from dev_dependencies
  dartz: ^0.10.0 # Moved from dev_dependencies
  dio: ^5.4.0 # Moved from dev_dependencies
  dio_cache_interceptor: ^3.5.0 # Moved from dev_dependencies
  dio_smart_retry: ^6.0.0 # Moved from dev_dependencies
  talker_dio_logger: ^4.0.2 # Moved from dev_dependencies
  url_launcher: ^6.3.0 # Moved from dev_dependencies
  webview_flutter_web: ^0.2.3 # Moved from dev_dependencies
  webview_flutter:
  universal_html: ^2.2.4 # Moved from dev_dependencies
  http: ^1.2.2 # Moved from dev_dependencies, updated version
  provider: ^6.1.2 # Moved from dev_dependencies
  flutter_inappwebview: ^6.1.5 # Moved from dev_dependencies
  custom_check_box: ^0.0.4 # Moved from dev_dependencies (assumed runtime)
  hive: ^2.2.3 # Latest compatible version
  hive_flutter: ^1.1.0 # Latest compatible version
  injectable:
  uuid: ^4.4.2
  web:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  build_runner: ^2.4.13
  freezed: ^2.3.4
  auto_route_generator: ^7.1.1
  flutter_launcher_icons: ^0.13.1 # Added version for clarity
  hive_generator: ^2.0.1 # Added for Hive adapter generation
  injectable_generator: ^2.6.2
  bloc: ^8.1.1 # Kept as dev_dependency (if only used for testing/generation)

flutter:
  uses-material-design: true
  assets:
    - assets/march_logo/
    - assets/
    - assets/program_assets/